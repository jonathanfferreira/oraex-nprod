# tnsnames.ora - Generated by Ansible
# Network Configuration File: {{ oracle_home }}/network/admin/tnsnames.ora

{{ db_name | default('ORCL') }} =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = {{ inventory_hostname }})(PORT = 1521))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = {{ db_name | default('ORCL') }}.local)
    )
  )

{{ pdb_name | default('ORCLPDB') }} =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = {{ inventory_hostname }})(PORT = 1521))
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = {{ pdb_name | default('ORCLPDB') }})
    )
  )

LISTENER_{{ db_name | default('ORCL') }} =
  (ADDRESS = (PROTOCOL = TCP)(HOST = {{ inventory_hostname }})(PORT = 1521))
