# listener.ora - Generated by Ansible
# Network Configuration File: {{ oracle_home }}/network/admin/listener.ora

LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = {{ inventory_hostname }})(PORT = 1521))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
    )
  )

SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (GLOBAL_DBNAME = {{ db_name | default('ORCL') }}.local)
      (ORACLE_HOME = {{ oracle_home }})
      (SID_NAME = {{ db_name | default('ORCL') | lower }})
    )
  )

ADR_BASE_LISTENER = {{ oracle_base | default('/u01/app/oracle') }}
